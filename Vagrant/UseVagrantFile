Nous allons voir comment utiliser un Vagrantfile

# Créer un dossier
$ mkdir stretch && stretch

# Initialisation du dépôt avec une box
$ vagrant init debian/jessie64
# Créer la VM
$ vagrant up

# Vérifie que la VM tourne
$ vagrant status
